const { resolvers } = require('./userSurvey.resolvers');
const { typeDefs } = require('./userSurvey.typeDefs');

module.exports = {
  userSurveyResolvers: resolvers,
  userSurveyTypeDefs: typeDefs,
};
