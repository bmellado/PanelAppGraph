const { resolvers } = require('./panelToken.resolvers');
const { typeDefs } = require('./panelToken.typeDefs');

module.exports = {
  panelTokenResolvers: resolvers,
  panelTokenTypeDefs: typeDefs,
};
